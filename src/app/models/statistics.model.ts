export class Statistics {
    constructor(
        public correctAnswers: number,
        public incorrectAnswers: number,
        public missedQuestions: number,
        public averageTime: number
    ) {}
}
